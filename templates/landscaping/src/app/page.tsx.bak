import { Nav, Hero, Services, About, WhyUs, Reviews, FAQ, ServiceAreas, Contact, Footer } from "@core/web"
import { config } from "@/lib/config"

export default function Home() {
  return (
    <>
      <Nav config={config} scrolledTheme="light" />
      <main>
        <Hero config={config} posterSrc="/hero-1.jpg" />
        <About config={config} imageSrc="/about-1.jpg" />
        <Services config={config} layout="zigzag" />
        <WhyUs config={config} />
        <Reviews config={config} ctaText={`Transforming yards since ${config.business.since} — ${config.business.review_count}+ happy homeowners`} />
        <FAQ config={config} />
        <ServiceAreas config={config} />
        <Contact config={config} heading="Get Your Free Landscape Design Consult" paragraph="Walk your yard with us — free assessment, design ideas, written quote." submitText="Schedule Free Consult" />
      </main>
      <Footer config={config} />
    </>
  )
}
