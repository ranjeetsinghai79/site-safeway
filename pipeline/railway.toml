[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node --import=tsx/esm src/reception/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "ai-reception"
