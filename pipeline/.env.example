# Google AI (Gemini — free tier, Gemini 2.5 Flash: 10 RPM / 250 RPD as of 2026-07-21,
# Google cut free quotas 50-80% on 2025-12-07 — re-check ai.google.dev/gemini-api/docs/rate-limits
# if this note gets stale)
GOOGLE_AI_API_KEY=AIza...

# Google Places API (New) — enable at console.cloud.google.com
GOOGLE_PLACES_API_KEY=AIza...

# Firecrawl cloud — firecrawl.dev (free 500 scrapes/month)
FIRECRAWL_URL=https://api.firecrawl.dev
FIRECRAWL_API_KEY=fc-...

# GitHub
GITHUB_TOKEN=ghp_...

# Cloudflare Pages
# Token: dash.cloudflare.com → My Profile → API Tokens → Create Token → "Edit Cloudflare Workers"
# Account ID: dash.cloudflare.com → Workers & Pages → right sidebar
CLOUDFLARE_TOKEN=...
CLOUDFLARE_ACCOUNT_ID=...

# Pipeline API URL (leave blank until you deploy the pipeline as an API)
PIPELINE_API_URL=

# Resend
RESEND_API_KEY=re_...
OUTREACH_FROM_EMAIL=hello@yourdomain.com

# Neon (managed serverless Postgres — neon.tech, free tier)
DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Pipeline config
# NICHE options: hvac | roofing | dentist | medspa | lawfirm | remodeling | cleaning | junk-removal | daycare | auto-detailing | restaurant
NICHE=hvac
LOCATION=Tracy, CA
CITY=Tracy
STATE=CA
COUNT=10
TEMPLATE_OWNER=ranjeetsinghai79
TEMPLATE_REPO=websitedeveloper
DEPLOY_OWNER=ranjeetsinghai79
DRY_RUN=true

# fal.ai — Flux Pro 1.1 image generation + Kling 1.6 Pro video generation (PRIMARY)
# ── Free stock media (images + videos) — used BEFORE fal.ai/HuggingFace ────────
# Register free at pexels.com/api (200 req/hr) — covers images + videos
PEXELS_API_KEY=

# Register free at pixabay.com/api — covers images + videos
PIXABAY_API_KEY=

# Register free at flickr.com/services/api — CC-licensed images (3600 req/hr)
FLICKR_API_KEY=

# Register free at freepik.com/api — photos (limited free tier)
FREEPIK_API_KEY=

# Register free at unsplash.com/developers (images only, 50 req/hr)
UNSPLASH_ACCESS_KEY=

# fal.ai — last resort paid fallback ($0.04/image, $0.098/video)
# Get key at fal.ai — $10 free credit on signup.
FAL_KEY=fal_...

# Hugging Face — Z-Image-Turbo + FLUX.1-schnell (FREE fallback if no FAL_KEY)
HUGGING_FACE_API_KEY=hf_...

# Google Service Account (for Sheets + GMB + GSC — optional)
# Save JSON file from GCP Console → Service Accounts → Keys → JSON as pipeline/service-account.json
GOOGLE_SERVICE_ACCOUNT_FILE=./service-account.json

# Google My Business (optional)
GBP_ACCOUNT_ID=accounts/123456789
GBP_LOCATION_ID=locations/987654321

# Google Sheets (optional — lead capture)
LEADS_SHEET_ID=

# Business owner email
BUSINESS_OWNER_EMAIL=pavan.harati@gmail.com

# ── Sales Funnel ────────────────────────────────────────────────────────────

# Twilio SMS (outreach + AI conversation agent)
# Get at twilio.com — $15 starter credit, ~$0.0079/SMS
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
TWILIO_FROM_NUMBER=+1XXXXXXXXXX
TWILIO_MESSAGING_SERVICE_SID=MGxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe (payment links + webhooks)
# Get at stripe.com — no monthly fee, 2.9% + 30¢ per transaction
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
WEBSITE_PRICE_ONE_TIME=29900   # cents = $299 one-time
WEBSITE_PRICE_MONTHLY=6900     # cents = $69/month founding-rate AI team bundle (all-inclusive)

# Calendly (meeting scheduling)
# Your personal Calendly link for 15-min discovery calls
CALENDLY_URL=https://calendly.com/your-name/15min

# ── Queue / Scale ───────────────────────────────────────────────────────────

# Parallel workers (how many sites to generate simultaneously)
# 3 = safe default, 10 = aggressive (watch fal.ai + Gemini rate limits)
WORKER_CONCURRENCY=3

# ── Tier system (v3) ────────────────────────────────────────────────────────

# Vapi (Scale tier — AI voice reception)
# Get at dashboard.vapi.ai — pay per minute of calls
VAPI_API_KEY=
VAPI_PHONE_ID=               # Phone number ID from Vapi dashboard (for inbound calls)
VAPI_DEMO_URL=               # Public URL to your AI Reception demo (sent in tier2 outreach)

# Slack (Grow+ — support channel + change request notifications)
# Create an incoming webhook at api.slack.com/apps
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Admin API token (for provisioning Scale API keys via POST /api/v1/keys)
ADMIN_API_TOKEN=your-secret-admin-token

# ── AI Reception — Vertex (paying clients) vs AI Studio (free demo) ────────────

# Comma-separated reception_configs.website_url values that use AI Studio (free
# tier) instead of Vertex AI for Gemini Live. Leave unset/empty to keep everything
# on Vertex (default, safe for paying clients — Vertex has no free tier and bills
# per audio-second from call #1).
AI_STUDIO_BACKEND_WEBSITE_URLS=

# AI Studio Live API model id. Confirmed 2026-07-21 via ai.google.dev Live API
# guide; differs from Vertex's GEMINI_LIVE_MODEL_VERTEX id — do not assume they match.
GEMINI_LIVE_MODEL_AI_STUDIO=gemini-2.5-flash-native-audio-preview-12-2025

# Second AI Studio API key (separate Google AI Studio project) — used as a backup
# once the primary GOOGLE_AI_API_KEY's daily usage crosses the warn threshold below,
# doubling free-tier headroom for demo traffic before falling back to paid Vertex.
GOOGLE_AI_STUDIO_WEBCREWAPI=AIza...

# Best-effort daily cap per AI Studio key + the fraction of it that triggers a
# switch to the next backend (default 90%). Gemini 2.5 Flash free tier is 250
# RPD as of 2026-07-21 — Live sessions may not map 1:1 to that number, so this is
# a conservative proactive guard, not an authoritative limit. The real safety net
# is gemini-live.ts's runtime fallback: any connect failure on one backend
# (quota, rate limit, or otherwise) immediately retries the next one in the
# chain, ending in Vertex — a call should never audibly fail from "out of credits."
AI_STUDIO_DAILY_CAP=250
AI_STUDIO_QUOTA_WARN_PCT=0.9
