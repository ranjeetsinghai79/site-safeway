export const runtime = 'edge'
import { BuilderApp } from "@/components/builder-app"

export const metadata = { title: "Website Builder — WebsiteDeveloper" }

export default function BuilderPage() {
  return <BuilderApp />
}
